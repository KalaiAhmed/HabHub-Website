{% extends 'base.html.twig' %}

{% block title %}Revue{% endblock %}

{% block body %}
    <h1>Back Office</h1>

    <h1>Revue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idrevue</th>
                <td>{{ revue.idrevue }}</td>
            </tr>
            <tr>
                <th>Nbetoiles</th>
                <td>{{ revue.nbetoiles }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ revue.commentaire }}</td>
            </tr>
            <tr>
                <th>Datepublication</th>
                <td>{{ revue.datepublication ? revue.datepublication|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_revue_index') }}">back to list</a>

    <a href="{{ path('app_revue_edit', {'idrevue': revue.idrevue}) }}">edit</a>

    {{ include('revue/_delete_form.html.twig') }}
{% endblock %}
