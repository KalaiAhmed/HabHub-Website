<form method="post" action="{{ path('app_revue_delete', {'idrevue': revue.idrevue}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revue.idrevue) }}">
    <button class="btn">Delete</button>
</form>
