<form method="post" action="{{ path('app_business_services_delete', {'idbusinessservices': business_service.idbusinessservices}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business_service.idbusinessservices) }}">
    <button class="btn">Delete</button>
</form>
