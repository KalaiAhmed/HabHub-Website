<form method="post" action="{{ path('app_annonce_adoption_delete', {'idannonceadoption': annonce_adoption.idannonceadoption}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce_adoption.idannonceadoption) }}">
    <button class="btn">Delete</button>
</form>
