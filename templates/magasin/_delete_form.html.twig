<form method="post" action="{{ path('app_magasin_delete', {'idmagasin': magasin.idmagasin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ magasin.idmagasin) }}">
    <button class="btn">Delete</button>
</form>
