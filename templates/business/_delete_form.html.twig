<form method="post" action="{{ path('app_business_delete', {'idbusiness': business.idbusiness}) }}"onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business.idbusiness) }}">
    <button class="btn">Delete</button>
</form>
