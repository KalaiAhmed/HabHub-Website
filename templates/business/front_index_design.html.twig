
        <div class="Grid--3" id="entitiesNav">

            {% for business in businesses %}

                <div class="grid-item" id="entitiesNav">


                            <a href="{{ path('app_business_show', {'idbusiness': business.idbusiness}) }}"><img src="{{asset('FrontOffice/uploads/'~business.image) }}" alt=""></a>




                            <span >{{ business.ville }}</span>
                            <span >03 August 2021</span>
                            <a href="#"><h4>{{ business.titre }}</h4></a>
                            <p>{{ business.horaire }}
                                {{ business.ville }}</p>
                            <div ><a href="#">Book Now</a></div>



        </div>

            {% endfor %}
        </div>

    <a href="{{ path('app_business_new_back_office') }}">Create new</a>
