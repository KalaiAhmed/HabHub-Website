{% extends 'base.html.twig' %}

{% block title %}Likes{% endblock %}

{% block body %}
    <h1>Likes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlike</th>
                <td>{{ like.idlike }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_likes_index') }}">back to list</a>

    <a href="{{ path('app_likes_edit', {'idlike': like.idlike}) }}">edit</a>

    {{ include('likes/_delete_form.html.twig') }}
{% endblock %}
